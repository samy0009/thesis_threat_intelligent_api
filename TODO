# Generate a hash code in order only the api will know if the querying is coming 
# from my user interface

    ☐ Generate hash function
    ☐ Validate the function on the api
    
# Initilize cached data

    ☐ Validate all the new data with the cached one

# Connect all the endpoints with the given apis

    ☐ Validate each api before connecting
    ☐ On data gathering filter every request